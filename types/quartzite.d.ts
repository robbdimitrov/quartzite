export function dateByAdding(type: string, date: Date, value?: number): Date;

export function timeDifference(now: Date, date: Date, style?: string): string;

export function dateDifference(ow: Date, date: Date, style?: string): string;

export function formatDate(date: Date, style?: string): string;
